Module Admin
    Public Const kLoginFailed As Integer = -1
    Friend Function ApplicationLogin(ByVal userName As String, ByVal password As String) As Integer
        ' Returns the users permissions if the given credentials are valid, else returns kLoginFailed.
        Dim permissions As Integer = 0

        Return permissions
    End Function
End Module
